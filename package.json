{
  "name": "chenglong-portfolio",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node scripts/build.mjs",
    "serve": "python3 -m http.server 4173",
    "check": "node --check app.js && node --check background.js && node --check scripts/build.mjs"
  },
  "dependencies": {
    "gray-matter": "4.0.3",
    "marked": "15.0.12",
    "sanitize-html": "2.17.0"
  }
}
